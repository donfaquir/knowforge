---
description: 模块拆分优先；非必要不膨胀 App.tsx / App.css
alwaysApply: true
---

# 前端壳层与样式边界（Knowforge）

## 组件与逻辑

- 新增功能、页面编排、状态与事件处理时，**优先**拆到 `src/components/`、`src/hooks/` 或按业务划分的子目录中的独立模块，**避免**把大块逻辑继续堆进 `src/App.tsx`。
- 只有确属「应用根壳层」的职责（例如全局布局入口、一次性路由级编排）才放在 `App.tsx`；能下沉的代码应下沉。
- 若必须修改 `App.tsx`：**改动尽量小**，并在回复中简要说明无法外提的原因。

## 样式

- 新增样式**优先**写在对应组件旁的样式文件，或现有与功能域绑定的 CSS 中。
- **非必要不要**在 `src/App.css` 中追加新选择器或大段规则；全局变量、跨区 chrome、与 `#root`/布局网格强绑定的样式可视为例外。
- 若必须改 `App.css`：**只加与本次需求直接相关的最小规则**，避免顺带「整理」无关样式。

## 目标

控制 `App.tsx` / `App.css` 体积，降低合并冲突与定位成本，使后续功能默认走模块化路径。
